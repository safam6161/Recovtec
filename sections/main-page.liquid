<section style="padding:80px 24px;max-width:760px;margin:0 auto">
  <h1 style="font-size:clamp(24px,3vw,36px);font-weight:700;margin-bottom:32px">{{ page.title }}</h1>
  <div class="rte">{{ page.content }}</div>
</section>

{% schema %}
{ "name": "Seite" }
{% endschema %}
