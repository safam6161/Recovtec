<nav class="nav" role="navigation">
  <div class="container nav-inner">

    <a href="{{ routes.root_url }}" class="nav-logo">
      <span class="mark" aria-hidden="true"></span>
      RECOVTEC
    </a>

    <div class="nav-links">
      <a href="{{ routes.root_url }}#problem">Warum Recovery</a>
      <a href="{{ routes.collections_url }}">Boots</a>
      <a href="{{ routes.root_url }}#howto">Anwendung</a>
      <a href="{{ routes.root_url }}#faq">FAQ</a>
    </div>

    <div class="nav-actions">
      <button class="icon-btn" aria-label="Suche">{% render 'icon', icon: 'search' %}</button>
      <button class="icon-btn" aria-label="Konto">{% render 'icon', icon: 'user' %}</button>
      <button class="icon-btn cart-btn" aria-label="Warenkorb öffnen" data-cart-open>
        {% render 'icon', icon: 'cart' %}
        <span class="cart-count" aria-live="polite" style="display:none">0</span>
      </button>
    </div>

  </div>
</nav>

{% schema %}
{ "name": "Header" }
{% endschema %}
